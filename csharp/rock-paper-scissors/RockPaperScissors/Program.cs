// Computer answer generation logic.

// Generate number, 0 (rock), 1 (paper), or 2 (scissors).
Random computer = new Random();
Console.WriteLine(computer.Next(0, 3));

// Map number to corresponding answer and assign variable